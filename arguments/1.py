# Write a function named combine that takes three positional arguments 
# and returns a tuple containing all three. Call this function with three different values.

def combine(x, y, z):
    return (x, y, z)